#!/bin/sh

if [ "$CI" = "true" ] || [ "$NODE_ENV" = "production" ]; then
    echo "CI or production environment, skipping Husky hook"
    exit 0
fi

. "$(dirname "$0")/_/husky.sh"
./node_modules/.bin/lint-staged --config ./node_modules/@jpapini/lint-staged-config/index.js

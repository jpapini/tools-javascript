#!/bin/sh

if [ "$CI" = "true" ] || [ "$NODE_ENV" = "production" ]; then
    echo "CI or production environment, skipping Husky hook"
    exit 0
fi

. "$(dirname "$0")/_/husky.sh"
$(dirname "$0")/../node_modules/.bin/lint-staged --config $(dirname "$0")/../config/lint-staged.config.js
